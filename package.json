{
  "name": "homebridge-curtain-stepper",
  "version": "0.1.0",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "prepack": "npm run build",
    "build": "tsc",
    "postpack": "npm run clean",
    "clean": "rm -rf lib"
  },
  "keywords": [
    "homebridge-plugin"
  ],
  "files": [
    "lib/**/*",
    "config.schema.json"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^16.11",
    "@types/node-fetch": "^2.6.1",
    "homebridge": ">=0.2",
    "typescript": "^4.5.5"
  },
  "engines": {
    "node": ">=0.12.0",
    "homebridge": ">=0.2.0"
  },
  "dependencies": {
    "node-fetch": "cjs"
  }
}
